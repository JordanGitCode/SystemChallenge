export interface PendingVersion {
  productId: string;
  versionId: string;
  versionNumber: number;
  name: string;
  description: string;
  price: number;
  sku: string;
  createdBy: string;
  createdAt: string;
}
